# ZenMoney API token. Get one from the ZenMoney web UI:
#   Settings → Applications → Create your own personal token
ZM_TOKEN=your_zenmoney_token

# Claude API key — used by the LLM categorizer for unknown merchants.
# Without it the CLI falls back to rule-based categorization only.
ANTHROPIC_API_KEY=your_anthropic_key
