REACT_APP_VERSION=$npm_package_version

# Public / demo-only build.
# No secrets are baked in — the bundle can only offer the persona picker.
# Used for endless.vyroslo.ru. Build with:
#   npm run build -- --mode demo
REACT_APP_DEMO_ONLY=true
